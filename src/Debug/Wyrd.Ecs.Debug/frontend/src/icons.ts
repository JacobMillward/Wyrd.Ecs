// Single import point for the lucide-static icons this UI uses. Each export is a raw
// <svg> markup string with stroke="currentColor", so it inherits whatever CSS `color`
// wraps it, needing no per-icon color wiring.
export {
    Play,
    Pause,
    RefreshCw,
    Filter,
    Columns3,
    ChevronUp,
    ChevronDown,
    Sun,
    Moon,
} from 'lucide-static';
